package dev.supergrecko.kllvm.contracts

/**
 * Common interface for all KLLVM Factories.
 *
 * A factory produces an instance of [P]. All public methods of [Factory] should produce [P].
 */
public interface Factory<P>